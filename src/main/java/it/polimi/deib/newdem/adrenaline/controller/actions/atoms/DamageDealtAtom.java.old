package it.polimi.deib.newdem.adrenaline.controller.actions.atoms;

import it.polimi.deib.newdem.adrenaline.controller.actions.atoms.iteractions.EntryPointFactory;
import it.polimi.deib.newdem.adrenaline.model.game.player.Player;

public class DamageDealtAtom extends AtomBase {

    // called for only one player
    private Player victim;

    public DamageDealtAtom(AtomsContainer parent, EntryPointFactory entryFactory) {
        super(parent, entryFactory);
    }
}
